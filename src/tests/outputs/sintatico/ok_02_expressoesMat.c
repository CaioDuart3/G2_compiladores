#include <stdio.h>
#include <stdlib.h>
#include <string.h>

    /* Declarações de variáveis globais */
    int a = 16;
    int b = 1;
    int c = 1;
    int d = 20;


int main() {
  b = 1;
  c = 1;
  a = (((6 + 2) / 2) * 4);
  d = (a + ((b + c) * 2));
    return 0;
}
